{
  "name": "intellitesting",
  "displayName": "IntelliTesting",
  "publisher": "hanssong",
  "description": "AI-driven unit test generation, execution, and validation with a zero-interruption workflow.",
  "version": "0.0.1",
  "icon": "icon.png",
  "license": "MIT",
  "engines": {
    "vscode": "^1.83.0"
  },
  "categories": [
    "Testing",
    "AI",
    "Other"
  ],
  "activationEvents": [
    "onCommand:extension.generateUnitTests",
    "onCommand:extension.showTestingChat"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "extension.generateUnitTests",
        "title": "Generate Unit Tests",
        "category": "IntelliTesting",
        "icon": "$(beaker)"
      },
      {
        "command": "extension.showTestingChat",
        "title": "Show Chat History",
        "category": "IntelliTesting",
        "icon": "$(comment-discussion)"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "extension.showTestingChat",
          "group": "navigation@1"
        },
        {
          "command": "extension.generateUnitTests",
          "group": "navigation@2"
        }
      ],
      "editor/context": [
        {
          "command": "extension.generateUnitTests",
          "when": "editorHasSelection",
          "group": "navigation@1"
        }
      ]
    },
    "configuration": {
      "title": "IntelliTesting",
      "properties": {
        "intellitesting.backendUrl": {
          "type": "string",
          "default": "https://intellitesting-backend-489299415304.europe-west1.run.app",
          "description": "Base URL of the AI backend API (e.g., http://127.0.0.1:8000). The '/generate_tests' path will be appended automatically."
        },
        "intellitesting.testExecutionCommand": {
          "type": "string",
          "default": "npm test",
          "description": "The command line to execute tests (e.g., 'pytest', 'mvn test', 'npm test')."
        },
        "intellitesting.projectTestPath": {
          "type": "string",
          "default": "src/tests",
          "description": "Default path where the generated test files will be saved."
        },
        "intellitesting.testFramework": {
          "type": "string",
          "default": "junit",
          "description": "The testing framework to use (e.g., 'pytest', 'jest', 'mocha'). Set to 'auto' for automatic detection."
        },
        "intellitesting.geminiApiKey": {
          "type": "string",
          "default": "",
          "description": "Your personal Gemini API key. If provided, requests bypass the daily rate limit. Get one free at https://aistudio.google.com/apikey"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src --ext ts"
  },
    "devDependencies": {
        "@types/vscode": "^1.83.0",
        "@types/mocha": "^10.0.3",
        "@types/node": "18.x",
        "@typescript-eslint/eslint-plugin": "^6.9.0",
        "@typescript-eslint/parser": "^6.9.0",
        "eslint": "^8.52.0",
        "glob": "^10.3.10",
        "mocha": "^10.2.0",
        "typescript": "^5.2.2",
        "@vscode/test-electron": "^2.3.6"
    }
}
